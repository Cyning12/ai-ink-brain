# Role: Senior AI Application Engineer (Next.js & AI Expert)

# Project Context: AI-Ink-Brain (RAG-Driven Personal Blog)
- **Frontend**: Next.js 15 (App Router), TypeScript, Tailwind CSS.
- **AI Stack**: SiliconFlow API, Vercel AI SDK, LangChain.js.
- **Database**: Supabase (PostgreSQL + pgvector).
- **Visual Style**: Minimalist Ink/Watercolor (Low saturation, large negative space).

# ==============================================
# 【一、_tech_graph 技术图谱强制规范（低幻觉）】
# ==============================================
1. 所有技术逻辑、架构、流程 必须基于 _tech_graph/ 下的 Mermaid 图谱
2. 禁止依赖大段纯文本文档作为开发依据
3. 前端独立维护自身 _tech_graph，不与后端混同
4. 图谱格式：
   - 全部为 .md 文件，内嵌 Mermaid 图表
   - 数据结构统一使用 Struct / classDiagram，禁止贴原始 interface / JSON
   - flowchart 遵循拓扑协议（见工作区根 AGENTS.md §7.2）
   - 边标记：-> 同步、~> 异步（Promise/await）、?> 条件、[ok]/[err] 状态、::xxx 元关系
   - 禁止裸边（无边标记的 -->）
   - 锚点用 // → path#Ln 独立注释行
5. 版本迭代必须标记，支持历史追溯
6. 采用「顶层总图 + 子流程按需加载」，不爆上下文
7. 图谱 = 前端唯一可信来源（Single Source of Truth）

# 本仓 _tech_graph 固定结构（双轨制）
_tech_graph/
├─ 00_main.md              # 全局路由 & 渲染流程（人类友好版）
├─ 00_main.ai.md           # 全局路由 & 渲染流程（AI 协议版）
├─ 01_struct.md            # TS 类型 & 数据结构
├─ 02_version.md           # 版本迭代时间线
├─ 10_flow_route.md        # 页面跳转流程（人类友好版）
├─ 10_flow_route.ai.md     # 页面跳转流程（AI 协议版）
├─ 11_flow_api.md          # API 请求 & 代理流程（人类友好版）
├─ 11_flow_api.ai.md       # API 请求 & 代理流程（AI 协议版）
├─ 12_flow_auth.md         # 登录 & 权限 & Session（人类友好版）
├─ 12_flow_auth.ai.md      # 登录 & 权限 & Session（AI 协议版）
├─ 13_flow_components.md   # 组件渲染 & 数据交互（人类友好版）
├─ 13_flow_components.ai.md # 组件渲染 & 数据交互（AI 协议版）
├─ 99_spec.md              # 前端实现规约
└─ 99_mermaid_protocol.md  # Mermaid 拓扑协议（引用后端仓规范）

# 双轨说明（全局标准，见工作区根 AGENTS.md §7）
# .md      = 人类维护版：简洁、裸边可用、锚点写在节点标签内
# .ai.md   = AI 协议版：结构化标记、零裸边、锚点分离、异步/错误显式标记
# 两者语义等价，flowchart 必须同时维护
# classDiagram / timeline / gantt 等无流程边的图，无需 .ai.md

# 注：后端 _tech_graph 结构见 ai-ink-brain-api-python/.cursorrules
# 全局统一规范见工作区根 AGENTS.md §7

# ==============================================
# 【二、图谱自动增量更新规则（核心）】
# ==============================================
1. 代码/页面/接口/逻辑变更 → 自动更新对应图谱
2. 仅做增量修改，不全量重绘、不破坏原有结构
3. 自动同步版本记录到 02_version.md
4. 代码与图谱必须始终保持一致，不允许偏差
5. 优先读取图谱进行开发，不脑补逻辑

# ==============================================
# 【三、Frontend Architecture Standards】
# ==============================================
1. **Next.js 15 规范**
   - 以 Server Components 为主，数据请求放服务端
   - Server Actions 用于表单操作、异步任务
   - 所有 AI/RAG 相关请求均代理到 Python 后端服务
2. **API 边界严格分离**
   - 前端只负责页面、组件、路由、代理转发
   - 不实现 Embedding、向量检索、分块、写入逻辑
   - 所有 AI 能力以 PY_API_URL 为唯一入口
3. **Streaming 输出**
   - AI 对话必须流式返回
   - 保持水墨 UI 打字机效果
4. **性能**
   - 优化 LCP
   - 懒加载非关键组件
   - 流式处理大文件

# ==============================================
# 【四、Data & RAG Standards】
# ==============================================
- 元数据必须保留：filename, original_link, section_header
- 向量检索使用 Cosine Similarity
- 支持 Hybrid Search（向量 + 全文检索）
- 分块策略遵循统一规范：size 512 / overlap 50

# ==============================================
# 【五、Visual Style Rules】
# ==============================================
- Tailwind CSS 唯一规范
- 水墨风格：低饱和、大量留白、优雅排版
- 背景：#F9F9F7
- 文字：#2C2C2C
- 强调色：低饱和靛蓝 / 石墨色
- 标题衬线字体，正文无衬线字体

# ==============================================
# 【六、Error & Stability Rules】
# ==============================================
- API / DB 请求必须 try/catch
- 向量库异常 → 降级为关键词搜索
- 开发环境输出 retrieval top-k 分数便于调试
- 统一优雅的错误状态展示

# ==============================================
# 【七、AI 防幻觉约束】
# ==============================================
- 不编造不存在的页面、组件、接口
- 不编造不存在的 TS 类型、数据结构
- 流程必须图形化，不使用自然语言讲故事
- 保持极简、高信息密度、低 Token 占用