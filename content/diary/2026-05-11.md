# 开发日记 · 2026-05-11

> **数据来源**：本地仓库 `git log`（`ai-ink-brain` **9** 条、`ai-ink-brain-api-python` **36** 条，`2026-05-11` 当天）及代表性提交的 **正文（`%B`）** 与 **`git show --stat`**。  
> **说明**：当日是 **ChatBI V3 / Text2SQL 可观测与澄清** 的「规格—契约—实现—留证—归档」高峰日；下文 **按主题展开**，完整 hash 列表可用：  
> `git log --since=2026-05-11 --until=2026-05-12 --reverse`（分仓执行）。

---

## 前端 `ai-ink-brain`（按大致时间顺序）

### 文档与任务索引（上午段）

| 提交 | 要点 |
|------|------|
| **`76ccc9b`** | 多轮 / rewrite prompt 等 **配对后端任务路径** 更新为 **`docs/tasks/done/`** 真值。 |
| **`47fe7c4` / `0cda040`** | **V3 Text2SQL 子阶段 SSE** 前端对接任务：范围、**数据源策略** 与验收口径 **写死**。 |
| **`7a1a1ad`** | **`docs/_tech_graph`**：Unified SSE **Text2SQL 子阶段** 与 **L1 子规 / 任务单** 交叉引用。 |
| **`36676ea`** | **§4.3 澄清** 前端任务单（**配对 P1-4** / 后端澄清单）。 |

---

### `7e25a9e` — feat：**SSE UX**、复制与 Timeline **批量展开 remount**

- **`UnifiedChatPageClient`**：流式处理打磨、**`done.ok=false`** 提示、**执行链 / Timeline 一键复制** 辅助函数。  
- **`ChainTimeline` / `ChainEventCard`**：通过 **`batchNonce` + `batchOpen`** 做 **批量展开后的稳定 remount**，缓解增量帧高频更新导致的 **折叠状态错乱**。  
- **任务**：**`task_frontend_unified_chat_streaming_sse_v1`** **迁入 `done/`**；增量 Timeline 任务 **指向后端已归档的 incremental SSE 任务**。

---

### `443931f` — feat：**V3 Text2SQL 子阶段 SSE** Timeline 与 **契约校验**

- **`lib/unified-chat/text2sqlPhaseSse.ts`**（新）：在 **`chainEventFromSse`** 路径校验 **`text2sql.phase.*`** 帧形态，非法即 **早失败 / 可诊断**。  
- **`ChainEventCard` / `ExecSection`**：**终态**侧重展示 **`output.text2sql_phases_ms`** 分段耗时，而不是把中间噪声全摊平。  
- **`types` / `UnifiedChatPageClient`**：接入子阶段事件消费。  
- **任务**：**`task_chatbi_v3_text2sql_phase_sse_timeline_frontend_v1`** 归档 **`content/tasks/done/`**；**`_views/done.md`** 更新。

---

### `40a51c6` — fix：**`meta.payload.run_id`** 与全链 **Timeline** 对齐

收到 **`meta`** 后采用 **服务端 canonical `run_id`**，并 **回填 `user.message`**，使 **`tool.call.end`**、**`done`** 与后端 **`CHATBI_JSON_LOG`** **同源**；任务单链接同步至后端 **done** 路径。

---

### `ec59622` — feat：Timeline **`agent.clarify`** 与 **P1-4** 前端任务

- **`types` / `ChainEventCard` / `ChainTimeline`**：**`agent.clarify`** 卡片与标签。  
- **`UnifiedChatPageClient`**：消费澄清事件帧。  
- **`docs/_tech_graph/11_flow_api.md`** 与 **`task_*_multiturn_clarify_semantics_4_3_frontend_v1.md`** 更新（与后端 §4.3 对齐）。

---

## 后端 `ai-ink-brain-api-python`

### A. V3 规格骨架与任务归拢（文档密集）

| 提交 | 要点 |
|------|------|
| **`b64d961`** | 新增 **`docs/spec/v3-agent/README.md`**、**`SPEC-ChatBI-V3-Overview.md`**（初版总规）；**active 任务** 与 **v2 / Enterprise Gap** 互链。 |
| **`8bd79e1`** | V3 **拆 L1 子规七份** 并更新总规索引与任务引用（Observability、Security、Identity 等目录雏形）。 |
| **`ab3e2f9`** | **V3 三单** 标注与 **SPEC §2.1** 批次对应（规划单 / 母单 / 子任务性质写清）。 |
| **`49eee4d` / `abf5d3e`** | **P0 Text2SQL** phased **A/B** 验收 spec、**RUNBOOK**、**env / PROJECT_CONFIG stub**。 |
| **`4fc0555` / `f07dc7b` / `3c8825d` / `9617de7`** | incremental SSE 任务 **收口与 manifest 门禁**；**后端 incremental 任务归档**；**multiturn grounding → done**；**rewrite timeline prompt capture → done** 与索引刷新。 |

---

### B. Text2SQL：**分阶段 SSE**、`phases_ms`、**JSON 日志** 与 **DISTINCT / 重试**

| 提交 | 要点 |
|------|------|
| **`397a323`** | **`tools.text2sql_execute`**：`retrieve` / `llm_sql` / `validate` / `db` / `llm_summary` **计时**，产出 **`text2sql_phases_ms`**；可选 **`chain_emit`** 发 **`text2sql.phase.start/end`**；**`asyncio.wait_for`** 分段超时；**`try_summarize_aggregate`** 快路径；对话上下文 **clip**。**`agent`** 向工具传 **`chain_emit`**；**`tool.call.end` output** 含 **`text2sql_phases_ms`**。**契约**：manifest + **Events §8.2**；**`tech_graph_contract_check`** 扩展扫描 **`api/tools.py`** 与 **`_t2sql_chain_dict`**。 |
| **`393755b`** | **`CHATBI_JSON_LOG`**：**`api/chatbi_json_log.py`**；**`chatbi.obs` 单行 JSON**（如 **`text2sql_phase_end` / `text2sql_tool_call_end`**），根字段 **`request_id` / `run_id`** 与 SSE 对齐；**`text2sql_phases_ms`** 与 ToolResult **同形**。 |
| **`79b2b14`** | **B-PR2 DISTINCT** 与 YAML values **并集**；**`execute_select_sql`** 可选 **`statement_timeout`**；Agent 路径 **value hints** 经 **`asyncio.to_thread`** 避免阻塞事件循环。**Supabase**：PostgREST 瞬断（**`ECONNRESET` / errno 54** 等）对 **insert/select** **指数退避重试**（**`rag_env`**、**`unified_chat` 落库**、**`agent_memory`**、**`database_manager`**）。文档 **`VALUE_HINTS_DISTINCT.md`**、种子 SQL；**SSE vNext** 异常路径 pytest 扩充。 |
| **`2c005ce`** | **批 JSON / 非增量 SSE** 路径向 **`agent.run`** **传入 `run_id`**，与 **RUNBOOK §3.4**、**`CHATBI_JSON_LOG` 整段打点** 一致。 |

---

### C. **`agent.clarify`**（低置信澄清短路）

**`5805b80`**：

- **`CHATBI_V3_LOW_CONFIDENCE_CLARIFY`** 门控 + **emit 早退**（可选 env，默认关闭安全）。  
- **`SPEC-ChatBI-V2-Events.md`**、**`_contract_manifest.json`**、**`docs/spec/v3-agent/P0/SSE-sample-agent-clarify.md`** 样例；任务与 **Multiturn-Debt** 交叉引用。

---

### D. P0 **阶段 A/B 留证、RUNBOOK 收口与归档**

多条 `docs(p0)` / `docs(chatbi-v3)` 提交（如 **`9ac17dd`、`7f23bc1`、`91df4a7`、`c479467`、`531228d`、`324a19b`** 等）共同完成：

- **curl SSE b-smoke**、**stderr grep 规范**（勿误 grep 留证 md）、**`text2sql_tool_call_end` tee** 等 **B3 闭环**留证。  
- **阶段 B 验收 md**（多份长文留档）、**P0/README** 扩至 **A+B**；**`task_chatbi_v3_text2sql_tool_latency_obs_v1` + RUNBOOK** **`git mv` → `done/`**；全仓链接改 **`tasks/done`**。  
- **`_contract_manifest`**：**`frontend_ts_ignore`** 增补 **`name` / `target`**，降低 **跨仓静态扫描误报**。

---

### E. 多轮 / 澄清 **文档链**

| 方向 | 提交示例 |
|------|----------|
| **Multiturn-Debt ↔ P0 观测** | **`5c99179`** |
| **§4.3 子任务登记与前端路径** | **`215d4b8`、`d488bf4`** |
| **Ink-Brain 前端任务链接** | **`bdc84cb`** |
| **Text2SQL 前端真值摘要 → L1/RUNBOOK/主任务** | **`9535a05`** |
| **Text2SQL 契约忽略键** | **`3a0e1d4`** |
| **intent 评测产物入 `tests/_out`** | **`be3d588`** |
| **V2 调用 query 留档（Timeline 导出）** | **`9823e8d`** |
| **澄清单 0–4 复用 / 阶段5 图谱 / P1-4 跑法** | **`31cb010`、`f9e0967`** |

---

### F. **Identity** 规格（文档）

**`bfe0258`、`7f65eb6`、`583a239`、`537cf4e`、`0a5ae00`**：四类用户与模块矩阵、**注册延后**、**超管本地密钥**、**临时 12h**、**分表校验**；**Bearer** 与前端 **input 边界**；**登录 exchange / 会话流**（MD5 vs SHA 纠偏）；**§5 个人项目迁移**、**§3.10 遗漏清单**、与 **超管 env** 对齐——为 **V3 Identity 实现** 划界，当日无强制代码合入要求。

---

## 当日技术点小结

- **子阶段可编排**：Text2SQL 从「黑盒一次 tool」拆成 **phase SSE + `phases_ms` + JSONL 单行观测`**，Timeline 与日志 **共享 `run_id`**。  
- **可靠性**：**DISTINCT 防漂移**、**Supabase HTTP 退避**、**SQL statement_timeout**、**to_thread** 避免阻塞，对应生产常见故障模式。  
- **澄清产品化**：**`agent.clarify`** 走 **可选 env 门控 + 契约样例 + 前端卡片**，衔接 **§4.3** 与 **Multiturn-Debt**。  
- **治理**：V3 **总规 + L1 子规 + RUNBOOK + P0 长留证**；任务 **`done/`** 与 **manifest `frontend_ts_ignore`** 同步，支撑 **跨仓 CI** 与 **人工验收** 同口径。

---

## 延伸阅读（仓库内）

- `docs/spec/v3-agent/SPEC-ChatBI-V3-Overview.md`、`docs/spec/v3-agent/README.md`  
- `docs/spec/v2-agent/SPEC-ChatBI-V2-Events.md` — §8 / **`agent.clarify`** / **`text2sql.phase.*`**（以文内版本为准）  
- `docs/_tech_graph/_contract_manifest.json`  
- `ai-ink-brain/lib/unified-chat/text2sqlPhaseSse.ts`  
- `ai-ink-brain-api-python/api/chatbi_json_log.py`、`docs/spec/v3-agent/P0/` 下 RUNBOOK 与留证说明
