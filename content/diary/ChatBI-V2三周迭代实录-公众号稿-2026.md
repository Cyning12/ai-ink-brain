# 三周把「规则问答」升级成可验收的 Agent：ChatBI V2 迭代实录

> **适合读者**：关注 LLM 应用工程化、SSE 流式体验、个人全栈项目的开发者与面试官视角的 HR。  
> **时间范围**：2026 年 4 月 29 日 — 5 月 10 日（正文聚焦该窗口内的 **V2 改进**；**5 月 11 日** 仓库内完成 **V2 文档与 Runner 收口归档**，下文「整体进度」与之对齐说明）。  
> **依据**：ChatBI V2 规格体系（总规目标、事件策略、验收与回归口径）与个人阶段总结《简历评估意见》中的 **V1 / V2 / V3 分层** 要求——**已完成与规划中不混写**。

---

## 一、为什么要做 V2？

V1 本质是 **「规则路由 + 固定分支」**：能跑通 RAG、Text2SQL 和流式输出，但遇到复杂问题时，**决策不可解释、多步协作弱、失败只能报错**。V2 在规格里定下的方向可以概括成五句话：

1. **由 LLM 参与意图与工具选择**，规则路由退居 **超时 / 低置信时的降级路径**。  
2. **用类 ReAct 的循环** 支持多步推理、工具切换与按失败类型的回退（含「无检索命中时是否允许转 SQL」等 **门控**）。  
3. **多轮记忆** 与会话落库可追溯，而不是「只记住最后一次提问」。  
4. **事件流兼容**：对外仍保留旧版模式语义，对内增加 **Agent 过程帧**，且 **契约化**（类型与最小字段可校验），避免前后端各说各话。  
5. **可验收**：除功能外，要有 **分级回归（L0～L7）**、样例输出与文档索引，方便自证「不是玩具 Demo」。

这与《简历评估意见》里对简历的提醒一致：**技术亮点要实、状态要分层**——V2 写的是「已里程碑完结」的能力，而不是把企业级 RBAC、熔断、多租户提前说成「已完成」。

---

## 二、这三周，V2 具体改进了什么？（按主题，不讲仓库路径）

### 1. Agent 骨架与「能讲清楚」的过程

- **4 月底**：把 **多步 Agent** 从纸面落到可跑：循环上界、工具封装、失败分支策略；流里出现 **步骤开始 / 思考摘要 / 意图结果 / 步骤结束 / 最终汇总** 等过程帧，并和 **契约表** 对齐，避免「后端多发了字段、前端不敢用」或反过来 **CI 一推就红**。  
- **价值**：面试官问「Agent 你怎么做的」，可以答 **决策循环 + 可观测事件 + 契约门禁**，而不是只答「调了 OpenAI API」。

**【此处插入截图 ①】**  
*建议：时间线（Timeline）一屏，能看到多步工具与 Agent 过程帧混排——打码会话内容即可。*

---

### 2. 路由可解释：从「黑盒 prefer」到「证据上屏」

- **4 月 30 日一带**：把 **路由证据** 做成独立事件，前端时间线可展示；默认 **落库追溯**，请求级调试开关再决定是否下发「候选列表级」细节，兼顾 **线上隐私** 与 **排障需要**。  
- 同步补齐 **路由全链路轨迹**、Text2SQL 执行摘要等，让「为什么走了 SQL / 为什么降级」能 **事后复盘**。

**【此处插入截图 ②】**  
*建议：同一条请求里「路由证据」与「最终决策」并列的界面截图。*

---

### 3. 流式与网关：假超时、断流、序列化雷区

- **5 月上旬**：长耗时阶段若 **长时间没有字节写出**，中间网关会误判超时——通过 **注释型保活帧**、把重落库挪到 **异步** 且不挡主 emit，稳住长连接。  
- 同时修了 **流结束时的边界 bug**、SQL 结果里 **非 JSON 友好类型** 导致整条 SSE 崩溃等问题；并对 **旧库表结构** 做了 **写入降级**（新列不存在时仍能插入核心字段），避免「一上线全挂」。

**技术点归纳**：SSE 不仅是「把字符串推出去」，而是 **背压、保活、序列化、兼容旧数据** 的组合题。

---

### 4. 意图层工程化：评测、缓存、Prompt 与历史窗口

- **Intent 评测**：固定用例集、导出表格/日志，**默认测试不把真实大模型评测打开**（避免 CI 或本地全量跑死），需要时再显式打开。  
- **缓存**：LRU + TTL、复合键（含历史摘要）、**命中/未命中与耗时** 可在时间线上看到，和规格里「可观测」一致。  
- **Prompt 与历史**：收紧「要执行查询 vs 问概念」的边界，限制 **历史条数与每条长度**，减少多轮指代时的胡答。

**【此处插入截图 ③】**  
*建议：同一条意图帧上展示「缓存命中 / 关键哈希 / 耗时」的调试视图（打码）。*

---

### 5. 增量流式体验：双栏、契约版本、契约校验「减误报」

- **5 月 8 日前后**：前后端约定 **流式协议版本号**（请求头声明），未升级客户端仍走 **整段回放**；升级后走 **增量 LLM 子阶段帧**，界面默认 **左时间线、右增量文本**，并把 **仅 UI 使用的辅助字段** 从「跨仓契约强校验」里剥离，减少 **假阳性漂移告警**。

**【此处插入截图 ④】**  
*建议：双栏布局 + 右侧可见逐字/逐段增量（动图更佳）。*

---

### 6. 多轮与 Text2SQL：语义承接与「值域提示」

- **5 月 9 日一带**：前端 **多轮 transcript**（已完成轮与当前轮分工）、**Strict 模式下的双写与 key 重复** 修复；后端 **多轮 grounding** 子模块与 **多轮语义承接** 子规落地，避免只靠 prompt「赌指代」。  
- **列值域 YAML**：把「枚举/取值提示」从 prompt 里抽成 **可版本化数据**，为后续 **DISTINCT 防漂移**、评测与回归打底。

---

### 7. 验收阶梯与「能自证」的 Runner（截至 5 月 10 日）

- **5 月 10 日**：把 **L0～L3** 写成可复制的命令与证据索引；**L4** 落 SSE 文本样例；**L5** 把失败矩阵类用例从「跳过」改为 **默认必须过**；**L6** 多轮 UI 侧验收成文；并在企业差距总览里 **单独立项**「工具链延迟观测」等 **V3 入口**，与 V2 交付边界划清。

**【此处插入截图 ⑤】**  
*建议：一页「验收层级勾选表」或 Runner 文档目录截图（无需暴露密钥）。*

> **说明（与 5 月 11 日关系）**：5 月 10 日当天已经把 **L0～L6 与大量留证、任务归档** 做到可对外讲述；**5 月 11 日** 在仓库内做了 **V2 文档验收归档与索引收口**——对外叙事可写为：**「V2 能力在 5 月 10 日前已跑通分级验收；文档与归档在次日封卷。」** 这样既诚实，又和《简历评估意见》里的 **「V2 里程碑已完结（2026-05-11 收口）」** 一致。

---

## 三、整体完成进度（对外怎么说才不自相矛盾）

结合规格中的 **V1→V2 目标** 与《简历评估意见》的 **分层建议**，建议用下面这张「口头甘特」对外说明（勾选表示 **截至 5 月 10～11 日已在个人项目中落地**）：

| 能力块 | 状态 | 备注 |
|--------|------|------|
| V1：RAG / Text2SQL / 规则路由 / SSE | ✅ 已完成 | 演示基座 |
| V2：ReAct + 工具 + 失败回退 / 门控 | ✅ 已完成 | 含软超时语义修正（避免多步死循环） |
| V2：LLM 意图 + 超时/低置信降级 V1 | ✅ 已完成 | 含评测与缓存可观测 |
| V2：多轮记忆与落库、过程事件契约 | ✅ 已完成 | 含跨端契约与 CI 门禁思路 |
| V2：增量流式 + 双栏时间线 | ✅ 已实现 | 协议版本开关，旧客户端可不退步 |
| V2：多轮语义承接 + 值域提示数据面 | ✅ 基线已交付 | 澄清策略、DISTINCT 节能等 **列入 V3 欠债与企业 Gap**，不并入「V2 已交付」夸大项 |
| 企业级：RBAC、语法级 SQL 防护、熔断限流、多租户 | 📋 V3 / V4 规划 | 与简历评估口径一致 |

**【此处插入截图 ⑥】**  
*建议：个人项目 README 或介绍页里「技术栈 + 仓库边界」一屏——用于证明全栈与分工清晰。*

---

## 四、实施中的坑与解决办法（故事版）

| 坑 | 现象 | 解决办法（可复述给面试官） |
|----|------|------------------------------|
| **长连接假超时** | Agent 或落库阶段很久不发字节，网关先断 | **保活帧** + **重 IO 异步化**，主线程专注 emit |
| **流式收尾偶发崩溃** | 结束包处理用到未绑定变量、或类型不能 JSON 化 | **收尾逻辑单测固化**；对结果表做 **安全序列化** |
| **Strict 模式双写** | 开发态正常、Strict 下 transcript 重复 | **不要在同一状态更新函数里混写副作用**；flush 后再读已提交状态；列表 key **不用纯内容当 key** |
| **评测拖垮 CI** | `.env` 误开真实大模型评测，pytest 全量卡死 | **测试夹子里优先清理意图评测环境变量**；真实评测改 **手动/独立流水线** |
| **契约校验误报** | 前端为布局加的辅助字段被当成「后端契约缺失」 | **UI 键从强校验集合剔除**（启发式 + 白名单），保留真字段门禁 |
| **「无检索命中就转 SQL」** | 浪费 SQL、体验差甚至有风险 | 规格里 **Gated**：只有结构化意图信号足够才允许转 SQL，否则追问或直接答 |
| **Agent 软超时误伤多步** | 每步都强制降级成规则路由，RAG 路径打转 | **仅在尚未执行任何工具前**允许超时降级，后续步交给失败处理器 |
| **旧环境表结构落后** | 新列不存在导致插入失败 | **检测列 → 降级插入路径**，关键诊断信息塞进已有 JSON 元数据 |

---

## 五、技术点清单（可用于公众号「要点卡片」）

- **架构**：自研轻量 Agent 循环，不绑重型编排框架；工具结果统一 **错误码 + 可序列化负载**。  
- **协议**：SSE 事件 **类型扩展 + 最小字段契约 + CI 静态扫描**，前后端可渐进升级。  
- **观测**：路由证据、执行分段耗时、单行结构化日志雏形、Runner **L0～L6** 分级证据链。  
- **体验**：增量子流、双栏、时间线稳定 key、调试态可复制会话标识。  
- **数据与 SQL**：多轮 grounding、列值域数据化、DISTINCT 与超时等「防漂移」组合（部分能力在 **V3 续作**）。  
- **工程文化**：规格—任务—验收—归档 **同一叙事**，和简历里「状态分层」要求对齐。

---

## 六、结语

这三周的本质，不是「多接了几个 API」，而是把 ChatBI 从 **能回答** 推进到 **能解释自己为什么这样答、失败时怎样换路、怎样在流式与网关上活下来、怎样用验收表证明不是 Demo**。

若你只记三句话给面试官：

1. **V1 是规则系统，V2 是带契约与验收的 Agent 化升级。**  
2. **企业级能力我写进 Gap 与 V3，不跟 V2 抢功。**  
3. **最难的是流式、观测与状态分层——我都落到文档和 Runner 里了。**

---

*本文档为公众号发布底稿，可按版面删节；截图占位请替换为实际运行界面（注意脱敏）。*
