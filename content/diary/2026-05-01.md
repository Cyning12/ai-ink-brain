# 开发日记 · 2026-05-01

> **数据来源**：本地仓库 `git log`（`ai-ink-brain`、`ai-ink-brain-api-python`）及提交正文（`%b`）；与 `git show --stat`、`.gitignore` 差异交叉核对。  
> **说明**：当日两条提交时间接近（约 10:45），体量上前端为小修、后端为 **大块语料与文档入库**；无跨仓契约变更主线。

---

## 前端 `ai-ink-brain`

### 提交：`9a02408` — fix(ui)：减少无用计算并稳定 postcss 导出

**动机**：降低 Unified / Chain Chat 结果展示路径上的 **重复渲染成本**，并消除 **PostCSS 配置在不同构建入口下导出形态不一致** 带来的隐患。

**改动要点**：

| 区域 | 内容 |
|------|------|
| **`SqlResultTable`** | 对 **`rows` 的 slice 结果** 使用 **`useMemo`**，避免父组件重绘时在表格路径上反复做相同切片与派生计算。 |
| **`SourceCitations`** | 移除 **未使用的 snippet 解析**分支，减少无效逻辑与潜在 dead code。 |
| **`postcss.config.mjs`** | 改为 **具名变量导出**再默认导出，使打包器/工具链对模块形状的解析更稳定（提交说明：避免构建期差异）。 |
| **图谱** | `docs/_tech_graph/02_version.md` 记一笔版本轨迹。 |

**涉及文件**：`components/chain-chat/SqlResultTable.tsx`、`components/SourceCitations.tsx`、`postcss.config.mjs`、`docs/_tech_graph/02_version.md`。

---

## 后端 `ai-ink-brain-api-python`

### 提交：`b456d75` — chore(text2sql)：追踪 DDL 语料与规格文档

**动机**：仓库根 `.gitignore` 原规则为 **`docs/*` 忽略**，仅通过 **`!docs/tasks/`**、**`!docs/_tech_graph/`** 放行部分目录；`docs/text2sql/` 若不显式放行，**大体积 DDL 与 init SQL 无法进 Git**，易出现「本地有、CI/生产 clone 后缺 **`supabase_init.sql`**」类问题。当日通过 **`!docs/text2sql/`** 解除忽略，并把语料与规格 **一并提交**。

**入库结构（节选）**：

| 路径 | 作用 |
|------|------|
| `docs/text2sql/v1/sql/*.sql` | 多业务表 **DDL 语料**（如 `agentinfo`、`claiminfo`、`policyinfo` 等），供 Text2SQL / schema 检索与评测对齐 **真实表结构**。 |
| `docs/text2sql/v1/sql/supabase_init.sql` | **初始化/聚合脚本**类真值；提交说明强调避免生产环境缺失该文件。 |
| `docs/text2sql/v1/spec/00_spec.md` | Text2SQL 模块 **SDD 式总规**（意图 → 专用 RAG → 生成 → 安全校验 → 执行 → 结果校验等章节提纲）。 |
| `docs/text2sql/v1/spec/EXEC-00-sql-prereq.md` | SQL 前置条件 / 执行环境说明。 |
| `docs/text2sql/v1/spec/SAMPLES-01-text2sql-mini.md` | 小型示例与「好题本」式参考。 |
| `docs/tasks/active/task_chatbi_v2_agent_p1c_intent_cache_observability_v1.md` | 与后续 P1 行为/可观测任务链的 **任务单落盘**（当日一并纳入版本库）。 |

**体量**：该提交统计约 **+8000 行** 级增量，主体为 **DDL 文件**，属 **数据/语料工程** 而非运行时逻辑大改。

---

## 当日技术点小结

- **前端**：在 **结果表与引用列表** 上做 **memo 与删冗**，配合 **PostCSS 导出形态稳定**，属于 **可合并、低风险** 的体验与构建卫生。  
- **后端**：通过 **`.gitignore` 例外 + 大文件进仓**，把 Text2SQL 依赖的 **DDL 与 init SQL** 变成 **与代码同源的真值**，降低「环境缺表结构」类故障；**v1 spec 三件套**（总规 / 前置 / 样例）为后续 **PR YAML 注入、B-PR DISTINCT** 等演进提供文档锚点（见之后日期的提交）。  
- **协作含义**：前后端当日无接口契约变更；后端工作偏重 **资产与规约落盘**，前端偏重 **展示路径性能与工具链稳定**。

---

## 延伸阅读（仓库内）

- `docs/text2sql/v1/spec/00_spec.md` — Text2SQL 总流程提纲。  
- `docs/text2sql/v1/sql/supabase_init.sql` — 初始化 SQL 真值。  
- 根目录 `.gitignore` — `!docs/text2sql/` 放行规则。
