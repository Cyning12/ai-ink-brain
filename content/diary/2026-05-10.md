# 开发日记 · 2026-05-10

> **数据来源**：本地仓库 `git log`（`ai-ink-brain`、`ai-ink-brain-api-python`）及提交正文；与 `git show --stat` 交叉核对。  
> **说明**：当日 **后端以 ChatBI V2 Runner（L0–L6）验收阶梯与测试门禁为主**；**前端仅 README 一次文档对齐**。整体偏 **「可复跑验收 + 证据落盘 + 任务 git mv 归档」**，代码逻辑改动集中在 **`9c50bfc`** 与 **`0539058`**。

---

## 前端 `ai-ink-brain`

### `8e3a8c0`（19:36）— docs(readme)：与当前技术栈、仓库边界及文档入口对齐

**`README.md`**（**+57 / -34**）重写为可执行的 onboarding 面：

- **技术栈表**：Next.js **16**（App Router）、React **19**、TypeScript、Tailwind **4**、**pnpm 10.x**、Node **24.x**（与 **`package.json` / `engines`** 对齐）。  
- **功能概览**：`/blog`、`/learning`、`/diary`、`/chat`、`/unified-chat`、`/chain-chat`、**BFF `app/api/py/**` → `PY_API_URL`**；可选 **本地 Node RAG** 的边界说明。  
- **仓库边界表**：本仓负责 **页面 / BFF / 展示**；**ingest、生产 RAG 主链路、向量 CI** 等归 **配对后端**；强调 **embedding 维度与 `supabase/sql/init.sql`** 须与后端一致。  
- **本地开发**：`pnpm install`、`cp .env.example .env.local`、`pnpm dev`（注明 **`next dev --webpack`**）；脚本表含 **`dev:turbo`**、**`lint`** 等。  
- **真值入口**：指向 **`docs/meta/PROJECT_CONFIG_AI_INK_BRAIN.md`**、**`.env.example`** 等，减少新人猜环境。

---

## 后端 `ai-ink-brain-api-python`

### `e67ab5b`（17:28）— docs(task)：Runner **§2.9 L0/L3** 验收日志

在 **`docs/tasks/active/task_chatbi_v2_acceptance_runner_v1.md`** 增补 **§2.9** 块（**+151** 行量级）：把 **L0（静态/契约）** 与 **L3（关键 pytest 子集）** 的 **命令、期望现象、证据索引** 写成 Runner 子文档，为后续 **`9c50bfc`** 的「固化门禁」提供文字锚点。

---

### `9c50bfc`（17:33）— test(chatbi)：固化 **L0** 与 **Intent / YAML** 测试门禁

| 主题 | 要点 |
|------|------|
| **`tests/conftest.py`** | **pytest 收集早于 `dotenv`** 时关闭 **`CHATBI_V2_INTENT_*`**，避免本地 **`.env` 打开真实 LLM 评测** 导致 **CI 或全量 pytest 卡死**。 |
| **`test_intent_agent_accuracy.py`** | **`_run_eval`** 在 **`real_llm`** 场景下临时对齐 **`CHATBI_V2_INTENT_LLM`**，使「评测路径」与「业务路径」可区分。 |
| **`test_text2sql_value_hints.py` + `text2sql_value_hints.py`** | **`utf-8-sig`**、**缓存清理**、**`importorskip("yaml")`**、失败时的 **诊断输出**，降低 Windows/编码差异导致的 flaky。 |
| **`_contract_manifest.json`** | **前端消费键**进一步纳入 **`frontend_ts_ignore`**，使 **跨仓 `contract_check`** 在增量 UI 键存在时仍 **稳定通过**。 |
| **Runner / `.env.example`** | 补充 **L0** 说明与 **`CHATBI_PYTEST_KEEP_INTENT_ENV`**（保留 intent 相关 env 的显式开关语义）。 |
| **导航** | **`docs/spec/v2-agent/README.md`** 更新；新增 **`docs/spec/SPEC-ChatBI-Enterprise-Gap.md`**（**+602** 行，企业 Gap 总览与索引，便于 L4–L7 与 v3 债项链回）。 |

---

### `2a394ae`（17:48）— docs(runner)：回填 **L1/L2** 验收勾选与指标摘要

在 **`task_chatbi_v2_acceptance_runner_v1.md`** 勾选 **L1/L2** 并写入 **指标摘要**（与当日验收节奏一致，改动面小但完成「阶梯不断档」）。

---

### `cc33795`（18:01）— docs(chatbi)：**L4 SSE** 归档、Runner 回填与 **`tests/_out`** 样本

- **Diary**：**`docs/diary/2026-05-10-l4-sse-acceptance-archive.md`**（L4 证据与结论收口）。  
- **Runner 任务单**：回填 **L4** 与 **SSE 样本**指针。  
- **`tests/_out/`**：新增 **`sse_sample_l4.txt`**、**`l6_turn1.txt`**、**`l6_turn2.txt`** 等 **可引用文本样本**；**`tests/_out/README.md`** 记一笔索引规则。

---

### `0539058`（18:10）— test(chatbi)：启用 **L5 canonical**；归档 **error_code** 矩阵

- **`tests/test_unified_chat_backend_v2_agent.py`**：移除对 **L5 canonical** 的 **skip**（**-4** 行），使 **FailureTypeHandler** 路径在默认套件中 **必须绿**。  
- **Diary**：**`docs/diary/2026-05-10-l5-failure-matrix-acceptance.md`**（**error_code** 矩阵与验收叙述）。  
- **`L5-ChatBI-V2-FailureTypeHandler-pytest指南.md`**：与 **unskip** 后的跑法 **交叉引用** 更新。  
- **Runner 任务单**：勾选 **L5 通过**。

---

### `f8233dc`（19:31）— docs(chatbi)：**L6 多轮**验收回填；企业 Gap 链 **v3 延迟观测**

- **Diary**：**`docs/diary/2026-05-10-l6-multiturn-ui-acceptance.md`**。  
- **`SPEC-ChatBI-Enterprise-Gap.md`**：**+1** 行级索引修补（与 v3 债项叙事衔接）。  
- **新任务**：**`docs/tasks/active/task_chatbi_v3_text2sql_tool_latency_obs_v1.md`**（**Text2SQL 工具链延迟观测**，承接企业 Gap 与 L6 之后的 **可运维指标**）。  
- **Runner**：回填 **L6** 勾选与说明。

---

### `31675a7`（19:56）— chore(tasks)：**P1** 与 **V2** 文档 **git mv** 至 **`done/`**

- **任务**：**`task_chatbi_v2_agent_p1_behavior.md`**、**`p1_eval_benchmark`**、**`p1c` / `p1d`** 等 **迁入 `docs/tasks/done/`**（**`git mv`**，保留历史）。  
- **归档说明**：**`task_chatbi_v2_docs_acceptance_archive_2026-05-11.md`**（文件名带 **05-11** 为归档批次标签，以仓库为准）、**`task_chatbi_v3_planning_after_resume_v1.md`**。  
- **索引**：**`docs/tasks/README.md`**、**`_views/done.md`**、**`AGENTS.md`**、**v2-agent README / Overview** 同步 **状态与链接**。  
- **顺带**：**`task_chatbi_v2_text2sql_multiturn_grounding_v1.md`** 小补丁（与 grounding 验收口径一致）。

---

## 当日技术点小结

- **Runner 真值化**：**L0→L3** 先有 §2.9 长文，再通过 **conftest + env 开关** 把「全量 pytest 不被 .env 误伤」写进基础设施。  
- **证据链**：**L4/L5/L6** 各有 **独立 diary md**，**`tests/_out`** 存 **SSE / 多轮** 文本样本，Runner 任务单 **逐层勾选**，满足 **可审计验收**。  
- **L5 从 skip 到 canonical**：**FailureTypeHandler** 回归进入 **默认门禁**，与 **2026-05-09** 的 Agent 超时语义修正形成 **前后依赖**。  
- **面向 v3**：**Enterprise Gap** 大文档 + **`text2sql_tool_latency_obs`** 任务，把 **企业侧缺口** 与 **延迟观测** 显式立项。  
- **前端**：README 与 **PROJECT_CONFIG**、**`PY_API_URL`** 边界对齐，降低 **跨仓 clone** 后的沟通成本。

---

## 延伸阅读（仓库内）

- `ai-ink-brain/README.md` — 技术栈与仓库边界（当日修订版）。  
- `ai-ink-brain-api-python/docs/tasks/active/task_chatbi_v2_acceptance_runner_v1.md` — Runner 与 L0–L7 勾选。  
- `ai-ink-brain-api-python/docs/diary/2026-05-10-l4-sse-acceptance-archive.md`、`2026-05-10-l5-failure-matrix-acceptance.md`、`2026-05-10-l6-multiturn-ui-acceptance.md` — L4/L5/L6 证据。  
- `ai-ink-brain-api-python/docs/spec/SPEC-ChatBI-Enterprise-Gap.md` — 企业 Gap 总览。  
- `ai-ink-brain-api-python/docs/tasks/active/task_chatbi_v3_text2sql_tool_latency_obs_v1.md` — v3 延迟观测任务。
