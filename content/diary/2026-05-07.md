# 开发日记 · 2026-05-07

> **数据来源**：本地仓库 `git log`（`ai-ink-brain`、`ai-ink-brain-api-python`）及提交正文；与 `git show --stat`、当前 `_contract_manifest.json` 中 **`agent.intent`** 最小键对齐。  
> **说明**：当日主线是 **P1-C Intent LRU+TTL 缓存与可观测**、**P1-D prompt/history 窗口**、以及 **v2-agent 总规 §7.4–§7.5 / L0–L7 文档与验收归档**；首对提交前后端 **时间戳相差约 2 秒**，属刻意配对合入。

---

## 前端 `ai-ink-brain`

### `ef42ba1`（11:25:18）— feat(unified-chat)：Intent 缓存可观测（`agent.intent` + 任务单 done）

**目标**：在 Unified Chat 的 **Timeline / Chain 卡片** 上，把后端透出的 **Intent 缓存命中语义** 与 **诊断字段** 可视化，便于对照 **P1-C** 任务验收与线上排障。

**`ChainEventCard`（`agent.intent`）**：

- 展示 **`tool` / `mode` / `confidence`**。  
- **缓存可观测**：**`cache`** 仅接受规范化取值 **`hit` | `miss`**（其余显示为 **「—」**），避免脏字符串误导。  
- **`cache_key_hash`**：非空则 **等宽展示**，便于 diff 两轮请求是否同一复合键。  
- **`latency_ms`**：数值则格式化为 **`N ms`**。  
- **`reasoning` / `fallback`**：收入 **`<details>`**，避免长 reasoning 挤占主时间线。

**其它改动**：`components/chain-chat/types.ts` 收窄/补齐与 **`agent.intent` payload** 相关的类型；**`UnifiedChatPageClient.tsx`** 增加与可观测/调试相关的客户端逻辑（与后端 `debug` 类开关协同，以仓库 diff 为准）；新增 **`content/tasks/done/task_frontend_unified_chat_agent_intent_cache_observability_v1.md`**；`docs/_tech_graph/02_version.md` 记版本。

---

## 后端 `ai-ink-brain-api-python`

### 1. `aa639d1`（11:25:20）— feat(unified-chat)：`agent.intent` payload 透传 cache 可观测字段

与前端 **`ef42ba1`** 配对：在 **`unified_chat`** 组装 **`agent.intent`** 时写入 **`cache`**、**`cache_key_hash`**、**`latency_ms`**（及与 intent 决策一致的已有字段）。  
**`_contract_manifest.json`**：将 **`agent.intent`** 的 **`payload_min_keys_by_type`** 扩展为包含 **`cache` / `cache_key_hash` / `latency_ms`**，使 **跨仓契约校验** 与 **前端读取上限** 同源，避免「后端多塞字段、manifest 未登记」导致 CI 或文档漂移。

---

### 2. `973c415`（11:39）— docs：归档 intent_eval 冻结轮并更新 P1-Eval 验收

- **产物**：`intent_llm_latest_20260507_113718_v1fb3_acc0933_macro0932.{jsonl,csv}` 纳入叙述与任务引用。  
- **Diary**：复跑三、对照表、**待网络项**勾选策略。  
- **P1-Eval / P1 总览**：**macro-F1**、RAG/多轮/稳定网络等验收口径；**准确率与缓存**相关勾选推进；**RAG 90% 口径**在总览中仍保留未勾状态（提交正文明示）。

---

### 3. `3cc1ebf`（15:36）— feat：P1-C Intent **LRU + TTL** 缓存与评测冻结轮切换

| 方向 | 内容 |
|------|------|
| **`intent_agent`** | **`history_hash` 复合键**（会话历史参与 cache key）、**`raw_response`** 在可观测路径下输出、**`DEBUG_INTENT_CACHE`** 控制调试粒度。 |
| **评测** | **基准脚本**支持 **冷/热两轮**；冻结验收迁至 **复跑五（`tout60`）** 等口径；**`tests/test_intent_cache.py`** 覆盖缓存行为。 |
| **资产** | **`tests/_out`** 归档 **复跑四/五** 的 JSONL+CSV。 |
| **文档** | 任务单、**`docs/diary/2026-05-06-p1-intent-benchmark.md`** 增补；**PROJECT_CONFIG** 补充 env。 |

---

### 4. `5a050ce`（15:38）— docs：新增 **P1-D** 任务单并链入 P1 总览

为 **Intent prompt 与阈值/历史窗口** 单独立项，避免与 P1-C 缓存实现混在一个验收口径里。

---

### 5. `4931d3d`（15:42）— fix(tech-graph)：`DEBUG_INTENT_CACHE` 与 manifest / drift CI 同步

- **`_manifest.json`**：登记 **`DEBUG_INTENT_CACHE`**。  
- **`99_spec.md` / `14_runtime_observability.md`**：补充 DEBUG 类开关说明，供 **`drift_check`** 与人类读者对齐。

---

### 6. `67542ad`（17:24）— docs(p1)：回归验收改为 **pytest 55 条**并勾选 D1/D2

把 **P1-D** 相关回归与 **统一 pytest 条数口径** 写死，减少「口头说全绿、实际只跑子集」的歧义。

---

### 7. `4cf8c0e`（17:44）— feat(chatbi)：P1-D Intent prompt 与 **history 窗口**，归档评测

- **`_llm_decide_v2`**：**6 条 × 200 字符** 历史窗口对齐；强化 **rag / text2sql / direct** 边界与 **多轮指代**。  
- **PROJECT_CONFIG**：**`INTENT_MIN_CONFIDENCE`**、超时 **`v1_fallback`** 等行为说明。  
- **任务单**：P1-D 验收勾选；归档 **`intent_llm_20260507_160444_p1d_live.*`**（含 **`.log`**）至 **`tests/_out`**。

---

### 8. `c9754ab`（17:46）— chore(git)：跟踪 **`.env.example`**

在 **`.gitignore` 仍忽略 `.env*`** 的前提下，**显式跟踪 `.env.example`**，便于 clone 后对齐 **新增 DEBUG / 超时 / 缓存类 env** 的键名与默认值说明。

---

### 9. `f5560c8` → `ac9fb67` → `3807f45` → `c07860c` — v2-agent 总规与 Runner 文档收口

| 提交 | 要点 |
|------|------|
| **`f5560c8`** | **总规 Overview**：**§7.4** 全量对照矩阵（后端/前端/DB/测试）；**§7.5** **L0–L7** 深度回归章节；验收勾选按总规口径；**Gap** 快照更新至 **2026-05-07**；**v2-agent README**、**P1 总览** 与 **P1-D** 引用同步。 |
| **`ac9fb67`** | **§7.5** 深度回归改为 **一行命令**写法，并拆分 **env** 与 **`-v` / `-s`** 说明，降低复制粘贴成本。 |
| **`3807f45`** | 新增后端 **`docs/diary/2026-05-07-l0-l3-regression-acceptance.md`**（L0–L3 表与证据索引）；Overview **§7.5.3–§7.5.6** 展开 **L4 SSE / L5 error_code / L6 跨仓 / L7 运维**；修正 **Intent 超时**表述；**§10** 链回 diary；Gap **下一步** 指向 §7.5.3–§7.5.6 与 diary。 |
| **`c07860c`** | **L4** 补充说明：**后台默认不打印逐条 SSE 控制台日志**（避免运维误以为「没流量」）。 |

---

## 当日技术点小结

- **可观测契约闭环**：后端 **`agent.intent` 扩展字段** ↔ **`_contract_manifest.json` 最小键** ↔ **前端 Timeline 展示**，同一真值链路过 CI 与 Code Review。  
- **P1-C**：**LRU+TTL**、**history_hash**、冷/热 bench、**`test_intent_cache`** 与多轮 **复跑产物** 归档，使缓存从「黑盒提速」变为 **可验收行为**。  
- **P1-D**：**历史窗口与 prompt 边界**落地，并与 **置信度阈值、超时 v1_fallback** 在 PROJECT_CONFIG 中可读；评测 **live 产物**进 **`tests/_out`**。  
- **治理**：**`.env.example` 进 Git**、**DEBUG_INTENT_CACHE 进 manifest**、总规 **L0–L7** 与 **独立 regression diary**，把「能跑」推进到 **能审计、能交接**。

---

## 延伸阅读（仓库内）

- `docs/_tech_graph/_contract_manifest.json` — `agent.intent` 的 `payload_min_keys_by_type`。  
- `docs/tasks/active/task_chatbi_v2_agent_p1c_intent_cache_observability_v1.md`、**`task_chatbi_v2_agent_p1d_intent_prompt_and_thresholds_v1.md`**。  
- `docs/spec/v2-agent/SPEC-ChatBI-V2-Agent-Overview.md` — §7.4 / §7.5。  
- `ai-ink-brain-api-python/docs/diary/2026-05-07-l0-l3-regression-acceptance.md` — L0–L3 验收索引。
