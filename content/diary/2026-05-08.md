# 开发日记 · 2026-05-08

> **数据来源**：本地仓库 `git log`（`ai-ink-brain`、`ai-ink-brain-api-python`）及提交正文；与 `git show --stat`、当前 `_contract_manifest.json`（**`agent.llm.*`** 与 `_note`）交叉核对。  
> **说明**：当日主轴是 **vNext 增量 SSE（`agent.llm.*`）** 的 **规格冻结 → 后端 G2 队列 emit → 前端双栏 + BFF 契约头 → 契约门禁与路由修复**；文档与代码在 **13:47 前后** 几乎同时合入，属 **前后端配对交付日**。

---

## 前端 `ai-ink-brain`

### 上午：`b794de4` — docs(tasks)：vNext 增量 SSE 前端任务与 v1 索引

- **`task_chatbi_v2_incremental_sse_timeline_frontend_v1`**：契约备忘与实现 PR 填空说明。  
- **`task_frontend_unified_chat_streaming_sse_v1`**：增加 **vNext 后续**索引，避免 v1 任务读者找不到增量主线。

---

### 下午：`4908f95` — feat：增量 SSE UI + **BFF 契约头透传**

- **`app/api/py/unified/chat/stream/route.ts`**：将 **`X-ChatBI-Sse-Contract`** **原样转发**至 Python 上游（与后端 **`Contract: 2`** 分支联动）。  
- **客户端**：发起请求时带 **`contract v2`**；解析 **chain 上 LLM delta**；**忽略顶层 token** 对子步的干扰（提交正文）。  
- **`ChainEventCard`**：增加 **`agent.llm.*`** 渲染；**Timeline 列表 key** 稳定化，减少增量更新时整表抖动。  
- **默认布局**：**左侧 Timeline + 右侧 LLM delta 面板**（`grid-cols-2`）；**移除 single-panel + LS 切换**（与 SPEC §6.1「当前固定双栏」对齐）。

**体量**：`UnifiedChatPageClient.tsx` 与链上组件合计 **+343 / -160** 行量级（`git show --stat`）。

---

### `cda9e42` — docs(task)：与 SPEC **§6.1** layout delta 对齐

- **对照表**：fixed dual column vs **single_panel + LS（deferred）**。  
- 更新 **范围、验收勾选、SPEC 指针**。

---

### `df1fbb7` — feat：vNext SSE Timeline、双栏与长篇任务文档

- **`UnifiedChatPageClient`**：**契约头**、**流式面板**、**`agent.llm.*`** 处理逻辑大块迭代。  
- **`ChainTimeline` / `ChainEventCard` / `ChainChatPageClient`**：LLM 子阶段 **标签与 layout hooks**。  
- **任务单**：前端 v1 任务更新。  
- **长篇说明**：新增 **`content/tasks/Timeline-Explanation.md`**、**`Timeline-Text2SQL.md`**（与 RAG 日记场景、Text2SQL 边界叙事配套，见下条）。

---

### `e3edfaf` — docs(tasks)：Timeline **RAG 日记场景**与 **Text2SQL 边界**

任务层补场景与边界说明，避免增量 Timeline 与 **非 SQL 类对话** 混测时验收口径发散。

---

## 后端 `ai-ink-brain-api-python`

### 上午：验收归档与 vNext 规格落地

| 时间 | 提交 | 要点 |
|------|------|------|
| 09:45 | **`dbb0a49`** | **Diary**：L4 SSE / 前端 Timeline **验收归档**（为当日增量工作提供「已验收基线」叙事）。 |
| 10:18 | **`a1c6f0e`** | **总规 Overview §11**：V2 里程碑暂结、**L5–L7 暂停集中验收**；链到 **vNext** 文档；新增 **`SPEC-ChatBI-V2-Incremental-SSE-Timeline-vNext.md`**（背景、UX、契约草案、验收）；**README / Gap** 与建议下一步同步。 |
| 10:50 | **`45d99d8`** | **vNext 增量 SSE 终稿**：Timeline SPEC 定稿、**§5 chain-only**、§7 可测验收、§8/§9、§8.1 DB 填空说明；**`SPEC-ChatBI-V2-Events.md` §8**、README 导航；manifest **`_note`**；**`PROJECT_CONFIG`** 增加 **`CHATBI_SSE_INCREMENTAL`**；澄清简报吸收索引与 **§8.8**；**后端增量 SSE 任务单**与实现备忘口径。 |

---

### 下午：`2ffdde4` — feat：经 **G2 emit 队列** 输出 **v2 增量 SSE**

**`ChatBIAgent.run`**：

- 支持 **可选 `emit` 回调**、**`sse_started_at` / `run_id`** 等元信息，与流式会话对齐。  
- **`chain-only` 的 `agent.llm.*`**：以 **模拟 chunk** 方式渐进输出（与后续真实上游流式可替换）；**intent/router 早帧**、**每步工具** 与 LLM 子阶段可在时间线上区分。

**`handle_unified_chat_stream`**：

- 当请求携带 **`X-ChatBI-Sse-Contract: 2`** 且环境 **`CHATBI_SSE_INCREMENTAL`** 开启时，走 **增量队列**路径；否则保持 **批量回放（batch replay）** 不变，降低对既有客户端的破坏面。

**契约与门禁**：

- **`_contract_manifest.json`**：登记 **`agent.llm.start` / `delta` / `end` / `truncated`** 及各自 **`payload_min_keys_by_type`**（例如 **`delta`** 至少 **`text` / `part_index`**，**`start`** 至少 **`phase` / `step_id`** 等）。  
- **`tech_graph_contract_check.py`**：静态扫描 **`api/agent.py`**（与 `unified_chat.py` 等 **合并为 `BACKEND_CONTRACT_SOURCES`**），避免 **emit 字符串里的 type** 与 manifest 漂移。

**测试**：新增 **`tests/test_unified_chat_sse_incremental_vnext.py`**（大体量，覆盖增量路径关键断言）。

**涉及文件**：`api/agent.py`、`api/unified_chat.py`、manifest、checker、**`02_version.md`**。

---

### 文档补丁：`7bdf412` / `1c80ffd`

| 提交 | 要点 |
|------|------|
| **`7bdf412`** | **§6.1 layout delta**：当前 **`ai-ink-brain` 固定双栏**（产品现状）与 **single_panel + LS**（目标态）对照表；Timeline SPEC **§3.2 / §7.5 / §9.3** 交叉引用；澄清简报 **§3** 表与索引指向 §6.1 与前端任务 diff。 |
| **`1c80ffd`** | **Events** 与增量 SSE 相关 **touch-ups**（与 §8 chain-only 叙事对齐，细节以 diff 为准）。 |

---

### 傍晚：`30c771b` — fix(intent-router)：`rag_rule_hits` 优先级与 **`no_data` gating**

- **RAG 规则命中**时 **优先 RAG 候选**（日记 / md / doc 等线索场景），避免被错误路由挤出。  
- 在 **RAG 路径**上 **推迟 `no_data` 判定**：直到 **DDL 与 FTS 均为空且缺乏 RAG 正向信号** 再落 **`no_data`**，减少「尚有检索语义却过早无数据」的误判。  
- **`tests/test_intent_router_backend_v1.py`** 增补；**`task_intent_router_backend_v1.md`** 回填 **2026-05-08** 验收描述。

---

### `da0fe2f` — chore(tech-graph)：SSE 契约校验 **忽略 TS UI 专用键**

- **Manifest**：增加 **`frontend_ts_ignore_payload_like_keys`**（ExecSection 等 **启发式 map** 上的 UI-only 字段）。  
- **Checker**：对契约键集合做 **减法**，再 **union** 进 **`contract_key_union`**，避免 **前端为布局存的辅助字段** 触发 **误报 drift**。

---

## 当日技术点小结

- **协议版本化**：**`X-ChatBI-Sse-Contract: 2`** + **`CHATBI_SSE_INCREMENTAL`** 把「是否增量」从隐式行为变成 **可观测、可关断** 的开关组合；v1 **batch replay** 路径保留。  
- **双轨 emit**：**G2 队列** 收敛 **高频 delta** 与 **背压**（配合后续 `agent.llm.truncated` 等帧）；**`agent.llm.*`** 进入 **manifest + 静态扫描**，与 **Events §8**、**vNext Timeline SPEC** 同源。  
- **前端体验**：**双栏默认**、稳定 **list key**、BFF **透传契约头**，使 **右栏增量拼接** 与 **左栏因果步** 可同屏验收。  
- **路由与门禁**：**Intent-router** 修正 RAG / `no_data` 竞态；**契约 checker** 忽略 **TS UI-only** 键，减少 **假阳性 drift**。

---

## 延伸阅读（仓库内）

- `docs/spec/v2-agent/SPEC-ChatBI-V2-Incremental-SSE-Timeline-vNext.md` — vNext Timeline 与验收。  
- `docs/spec/v2-agent/SPEC-ChatBI-V2-Events.md` — §8 `agent.llm.*`。  
- `docs/_tech_graph/_contract_manifest.json` — `agent.llm.*` 与 `_note` 中 **BACKEND_CONTRACT_SOURCES** 说明。  
- `ai-ink-brain/content/tasks/Timeline-Explanation.md`、`Timeline-Text2SQL.md` — 场景与边界长文。  
- `ai-ink-brain/app/api/py/unified/chat/stream/route.ts` — **`X-ChatBI-Sse-Contract`** 转发。
