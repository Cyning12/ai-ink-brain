# 开发日记 · 2026-05-13

> **数据来源**：工作区根目录 `git log --since=2026-05-13 --until=2026-05-14`、`ai-ink-brain/` 与 `ai-ink-brain-api-python/` 两子仓同日 `git log`（作者时区含 +0800）。路径均相对各仓根目录，不写机器绝对路径。若某子目录无独立 `.git`，其 `git log` 会沿父级解析，以下以子仓独立仓库为准。

---

## 一、总览：当日主线

| 主题 | 位置 | 一句话 |
|------|------|--------|
| **Harness V2 与 P0 文档骨架** | 工作区根 `docs/harness/`、`AGENTS.md`、`.gitignore` | 初版 P0 验收单与规划、测评文、README；合并前检查与 `pnpm test` / quality CI 对齐；忽略 `.pnpm-store`、`.cursor`。 |
| **Harness 提示词与任务视图** | 工作区根 | 新增 `docs/harness/prompts/*`、任务 `_views`、VERIFICATION 模式说明、`DIARY_GUIDE.md`、根 `README` 与 `docs/meta/`。 |
| **ChatBI V3 预览与执行令牌** | api-python + ink-brain | 低置信 SQL 预览、`plan_execution_token` 放行与默认确认开关；前端消费 `agent.plan.preview` 与 TTL / SSE 刷新。 |
| **文档与审查闭环** | api-python | 任务迁入 `done/`、P1-1 SQL AST / P1-2 Prompt 注入任务单、Security 审查 R2/R3 与 `review_results` 归档、P1-4 规格与手测日志。 |
| **CI 与质量门禁** | 两子仓 | 后端：`pytest` + `verify-fast`、SSE 契约门禁修复、Actions Node 24；前端：Harness P0 lint+build、Vitest 质量门、可选 verify-fast、Actions v5 / Node 注解清理。 |
| **合仓** | api-python | 合并 PR **#20**、**#21**（`feat/chatbi-v2-agent`）。 |

---

## 二、工作区根（cyning-ink-workspace 顶层 Git）

按时间顺序（摘要来自提交说明）：

1. **`5588469`** — `docs(harness)`：新增 P0 验收单、V2 规划、测评文档与 harness `README`；新增根 `.gitignore`。
2. **`e8e4b46`** — `chore(git)`：忽略 `.pnpm-store` 与 `.cursor`，减少无关文件进入版本库。
3. **`3c78e1f`** — `docs(harness)`：合并前检查清单与 **`pnpm test`**、`quality` CI 步骤对齐。
4. **`ae84cf7`** — `docs(workspace)`：根 `README.md`、`DIARY_GUIDE.md`、`docs/meta/`（含 GitHub 建库字段备忘）；`AGENTS.md` 范围收敛为工作区根；harness 下 prompts、tasks 视图、`VERIFICATION_CI_PATTERN.md`、`reviews`/`tasks` 索引；修订 plan / acceptance / 测评引用与链。

**技术点**：以文档与仓库卫生为主，建立 **Harness 提示词模板**、**任务看板 Markdown 视图**、**CI 验证模式** 的可复用落盘，便于后续 Agent 与人类按同一套清单验收。

---

## 三、后端 `ai-ink-brain-api-python`

### 3.1 功能与行为

- **V3 低置信 Text2SQL**：预览链路、**`plan_execution_token`** 放行；**`CHATBI_V3_PLAN_PREVIEW_CONFIRM`** 默认开启相关修复（见当日 `fix(chatbi)` 提交）。
- **P1-4 澄清短路（方案 B）**：实现与任务/规格、联调留档一并合入。
- **`unified_chat` / SSE**：队列下限与背压相关单测稳定性修复。

### 3.2 文档与 Harness

- 已完成任务整理至 **`docs/tasks/done/`**，总规交叉链接更新。
- ChatBI：**分级闸门**任务关单、text2sql 有/无权限输出索引。
- 新增 **P1-1（SQL AST）**、**P1-2（Prompt 注入）** 实施任务单；补充 **`failure_paths`** 等 Harness 字段。
- Security：**deny_code 冻结**、P1-1/P1-2 验收收紧；**R2 / R3** 审查与复检目录归档。
- P1-4：低置信强执手测日志写入 spec。

### 3.3 CI

- Workflow：**`pytest`** 与 **`verify-fast`**（Harness V2）。
- **SSE 契约静态扫描**误报修复；GitHub Actions **Node 24** 对齐。

### 3.4 合并

- **`6edd40d`** Merge PR **#20**、`fcbc040` Merge PR **#21**（分支 `feat/chatbi-v2-agent`）。

**技术点**：ChatBI V3 在「低置信 → 人可确认 → 带令牌执行」上前后端对齐；后端侧补强 **文档—任务—审查结果** 的可追溯链；CI 上 **契约门禁 + 双轨 workflow（全量 pytest / 快速 verify）** 降低回归成本。

---

## 四、前端 `ai-ink-brain`

### 4.1 Unified Chat / Plan

- 消费 **`agent.plan.preview`**，下发 **`plan_execution_token`**。
- **Plan TTL**：`useEffect` 依赖补全；在 token 丢弃或 TTL 过期后 **刷新 SSE**。
- 任务：`plan_execution_token` 相关前端任务归档至 **`content/tasks/done/`**。
- 文档：分级闸门、intent router 等任务单路径更新为 **`tasks/done`**。

### 4.2 Harness 与 CI

- **`quality`** workflow：Harness **P0**（lint + build）。
- **Vitest** 质量门与最小 **`lib`** 单测；**`packageManager`** 单一真源（移除 workflow 中冗余 `pnpm version`）。
- 可选 **`verify-fast`** workflow；Actions **v5**、去掉 Node 20 注解类问题修复。
- Harness P1 Vitest 任务关单与人工验收签注；任务目录增加 **review / reinspect_results** 与 **MDX 排除** 说明。

**技术点**：前端与后端 V3 **plan 预览 + 执行令牌** 契约对齐；仓库内建立 **Vitest 可失败门禁** 与 **可选快速验证 job**，与根目录 Harness 文档形成上下文集。

---

## 五、跨仓小结（便于次日接续）

- **产品链路**：低置信 SQL → 预览与确认 → `plan_execution_token` → 执行；前后端同日多笔提交闭环。
- **工程化**：工作区根补 **Harness 操作面**（提示词、验收、CI 模式）；子仓补 **自动化门禁**（pytest、Vitest、verify-fast）与 **契约扫描修复**。
- **安全与治理**：P1-1 / P1-2 任务落地、Security 审查多轮归档，为后续 AST 闸门与注入防护实施提供工单与验收锚点。

---

## 六、未在 Git 中体现的部分

本地编辑未提交、仅 IDE 或构建产物变更不会出现在上述 log 中；若需「含未提交改动」的日报，需另行 `git status` 或对比工作区时间戳。
