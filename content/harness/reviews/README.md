# content/harness/reviews（前端仓 · 任务审核产出）

> **用途**：`content/tasks/` 相关 Harness **任务审核帽（22）** 书面结论全文。  
> **说明**：本仓 `docs/*` 默认 gitignore（仅 `docs/_tech_graph/` 例外），故 reviews 与 invokes 同放在 **`content/harness/`**。  
> **通则**：工作区 `Projects/docs/harness/prompts/22-task-audit.md` · `Projects/docs/harness/reviews/README.md`。

---

## 命名

- 推荐：`task_<slug>_audit_R<轮次>_YYYYMMDD.md`  
- 新 task 可选子目录：`reviews/by-task/<task_slug>/`（与 invoke taxonomy 对称）

**工作区指针**：`Projects/docs/harness/reviews/pointer_task_<slug>_audit_*.md` 可链回本目录真值，避免双份漂移。

---

## 与 `content/tasks/review_results/` 的分工

| 目录 | 帽 | 说明 |
|------|-----|------|
| `content/harness/reviews/` | **22** 任务审核（Harness） | 本目录 |
| `content/tasks/review_results/` | **20** 规格短评等 | 见该目录 README |

---

## 给 Cursor

`22`、`task_audit`、`reviews`、`human_gate`
